export type ContributorType = {
    avatar : string,
    name: string,
    commits: number,
    github: string
}

export type RepoType = {
    name: string,
    forked: boolean,
    stars: number,
    lastUpdated: string
}