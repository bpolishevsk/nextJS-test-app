import { RepoType } from '../utils/types';

function RepoCard({ repoItem }: { repoItem: RepoType}) {
    return ( 
        <>
            <div className='flex flex-col'>

            </div>
        </>
     );
}

export default RepoCard;